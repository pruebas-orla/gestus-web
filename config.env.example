# Archivo de ejemplo de configuración
# Copia este archivo a config.env y completa los valores

# Configuración del servidor
PORT=5000

# Configuración JWT
JWT_SECRET=tu-secret-key-muy-seguro-aqui
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d
JWT_ALGORITHM=HS256

# Configuración de MySQL
# Para desarrollo local (Docker Compose)
DB_HOST=mysql
DB_PORT=3306
DB_NAME=webprojectdb
DB_USER=gestus
DB_PASSWORD=gestuspassword

# Para Cloud SQL (usar en producción)
# DB_HOST=/cloudsql/PROJECT_ID:REGION:INSTANCE_NAME
# DB_PORT=3306
# DB_NAME=webprojectdb
# DB_USER=gestus
# DB_PASSWORD=se-configura-como-secreto

# Configuración Firebase Admin
FIREBASE_DATABASE_URL=https://gestusproject-default-rtdb.firebaseio.com

# Opción 1: Ruta al archivo JSON (solo para desarrollo local)
FIREBASE_SERVICE_ACCOUNT_PATH=./google-services.json

# Opción 2: JSON como string (recomendado para producción/Cloud Run)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"..."}

